import { Theme, Themes } from './types';
declare type ThemeMapping = {
    [propertyName: string]: Theme;
};
export declare const defaultThemes: ThemeMapping;
export declare function createTheme<T>(name?: Themes, customTheme?: T): Theme;
export {};
