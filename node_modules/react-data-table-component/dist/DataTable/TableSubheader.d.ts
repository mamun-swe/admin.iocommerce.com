import * as React from 'react';
declare type AlignItems = 'center' | 'left' | 'right';
declare type TableSubheaderProps = {
    align?: AlignItems;
    wrapContent?: boolean;
    children?: React.ReactNode;
};
declare const TableSubheader: ({ align, wrapContent, ...rest }: TableSubheaderProps) => JSX.Element;
export default TableSubheader;
