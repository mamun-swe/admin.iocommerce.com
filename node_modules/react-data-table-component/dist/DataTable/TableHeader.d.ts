import * as React from 'react';
import { Direction } from './constants';
import { ContextMessage } from './types';
declare type TableHeaderProps = {
    title?: string | React.ReactNode;
    actions?: React.ReactNode | React.ReactNode[];
    direction: Direction;
    selectedCount: number;
    showMenu?: boolean;
    contextMessage: ContextMessage;
    contextActions: React.ReactNode | React.ReactNode[];
    contextComponent: React.ReactNode | null;
};
declare const TableHeader: ({ title, actions, contextMessage, contextActions, contextComponent, selectedCount, direction, showMenu, }: TableHeaderProps) => JSX.Element;
export default TableHeader;
