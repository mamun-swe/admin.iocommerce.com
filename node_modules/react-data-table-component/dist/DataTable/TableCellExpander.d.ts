import { ExpandableIcon } from './types';
declare type TableCellExpanderProps<T> = {
    disabled: boolean;
    expanded: boolean;
    expandableIcon: ExpandableIcon;
    id: string | number;
    row: T;
    onToggled: (row: T) => void;
};
declare function TableCellExpander<T>({ row, expanded, expandableIcon, id, onToggled, disabled, }: TableCellExpanderProps<T>): JSX.Element;
export default TableCellExpander;
