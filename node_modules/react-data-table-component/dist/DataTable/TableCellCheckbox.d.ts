import * as React from 'react';
import { RowRecord, RowState, SingleRowAction, ComponentProps } from './types';
declare type TableCellCheckboxProps<T> = {
    keyField: string;
    row: T;
    rowCount: number;
    selected: boolean;
    selectableRowsComponent: 'input' | React.ReactNode;
    selectableRowsComponentProps: ComponentProps;
    selectableRowsSingle: boolean;
    selectableRowDisabled: RowState<T>;
    onSelectedRow: (action: SingleRowAction<T>) => void;
};
declare function TableCellCheckbox<T extends RowRecord>({ keyField, row, rowCount, selected, selectableRowsComponent, selectableRowsComponentProps, selectableRowsSingle, selectableRowDisabled, onSelectedRow, }: TableCellCheckboxProps<T>): JSX.Element;
export default TableCellCheckbox;
